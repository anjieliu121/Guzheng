torch>=2.5.1
numpy>=1.26.4
mido>=1.3.3

# NotaGen + ABC processing (notagen/ subdir)
transformers>=4.40.0
abctoolkit>=0.0.6
pretty_midi>=0.2.11

# Hugging Face — load published checkpoints
safetensors>=0.7.0
huggingface_hub>=0.36.2

# System dependency (install separately):
#   macOS:   brew install abcmidi
#   Ubuntu:  apt-get install abcmidi
# Provides `abc2midi`, used by notagen/generate.py and baseline/generate.py
# to render ABC output as MIDI.
